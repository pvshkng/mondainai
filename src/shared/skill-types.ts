export type SkillEntry = {
  id: string
  name: string
  description: string
  content: string
  active: boolean
}

export type SaveSkillInput = {
  name: string
  description: string
  content: string
  active: boolean
}
